
  # Convert to Figma file

  This is a code bundle for Convert to Figma file. The original project is available at https://www.figma.com/design/zG5l4giia8ztDcOXHx2yYA/Convert-to-Figma-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  